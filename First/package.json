{
  "name": "function-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rimraf dist && tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "bundle": "npm run build && cd dist && bestzip ../bundle/academy-functions.zip *",
    "deploy": "az functionapp deploy --resource-group Akademia2 --name test-azure-functions-valentin --type zip --src-path bundle/academy-functions.zip",
    "test": "echo \"No tests yet...\""
  },
  "devDependencies": {
    "@azure/functions": "^1.2.3",
    "@types/node": "^15.12.5",
    "typescript": "^4.0.0",
    "bestzip": "^2.2.0",
    "rimraf": "^3.0.2"
  }
}
